schema_version: 1
id: zettaserver
name: ZETTASERVER
type: server
site: nmc-belleville
role: broadcast-automation

network:
  mgmt_ip: 192.168.100.97
  mgmt_port: 3389
  notes: Windows Server 2012 R2 (per notes).

access:
  mgmt_url: rdp://192.168.100.97
  auth_ref: 1password://Shared/SERVER - ZETTASERVER Admin
  notes: >
    Confirm services and any dependencies that may traverse ISP (IP-based STL, vendor licensing, remote control).

ownership:
  primary: dj
  escalation: dt

dependencies:
  upstream:
    - sonicwall-nmc
  downstream: []

cutover:
  criticality: broadcast-critical
  prechecks:
    - Confirm Zetta-related outbound connectivity requirements (if any) via Bell
    - Confirm any remote access paths are validated
  rollback:
    - Revert WAN routing to Cogeco if Zetta comms fail

evidence:
  pre_change_dir: evidence/pre-change/zettaserver/
  post_change_dir: evidence/post-change/zettaserver/
